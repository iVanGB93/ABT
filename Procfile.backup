# Emergency Procfile - if config file fails
# Rename this to Procfile if gunicorn.conf.py causes issues

web: gunicorn core.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --log-file - --access-logfile -
